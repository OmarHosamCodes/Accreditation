{
  "name": "accreditation",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "engines": {
    "bun": ">=1.3.0"
  },
  "scripts": {
    "dev": "bun --hot --filter @accreditation/server dev",
    "start": "bun --filter @accreditation/server start",
    "build": "bun --filter @accreditation/server build",
    "test": "bun test",
    "typecheck": "bun --filter @accreditation/shared typecheck && bun --filter @accreditation/server typecheck && bun --filter @accreditation/web typecheck",
    "check": "bun run typecheck && bun run build"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "typescript": "^5.8.0"
  }
}
