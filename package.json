{
  "name": "accreditation",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run index.ts",
    "dev": "bun --watch index.ts",
    "check": "bun build index.ts --target=bun --outfile=/tmp/accreditation-check.js"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "postgres": "^3.4.9"
  }
}
