{
  "name": "@accreditation/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot src/index.ts",
    "start": "bun src/index.ts",
    "build": "rm -rf dist && bun build src/index.ts --target=bun --outdir dist --minify",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@accreditation/shared": "workspace:*",
    "postgres": "^3.4.9"
  }
}
