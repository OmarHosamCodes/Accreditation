# Postgres connection string (optional locally; required in production)
DATABASE_URL=postgres://user:password@localhost:5432/accreditation

# HTTP Basic Auth credentials for admin and extension API access
ADMIN_USERNAME=roaster
ADMIN_PASSWORD=change-me

# Server port (default: 3000)
PORT=3000

# Set to production in deploy environments
NODE_ENV=development
